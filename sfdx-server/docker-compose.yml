version: '3.8'

services:
  sfdx-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - API_KEY=${API_KEY}
      - NODE_ENV=production
    volumes:
      - ./projects:/app/projects
